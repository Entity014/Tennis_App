version: '3.8'

services:
  lock-server:
    build: .
    container_name: lock-server
    ports:
      - "3000:3000"
    volumes:
      - ./data:/usr/src/app/data
    restart: always
    environment:
      - NODE_ENV=production
