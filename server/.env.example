# Configuration for Local Kiosk Server (running on NAS)

# URL of the booking server running on Railway
BOOKING_SERVER_URL=http://localhost:3001

# Secret shared with the Railway booking-server for kiosk API authentication
KIOSK_SYNC_SECRET=your_kiosk_sync_secret_key_here
